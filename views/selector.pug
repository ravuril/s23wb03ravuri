extends layout

block content
  h1= title
  -let results =["mahindra-thar.jpg","tata_safari_off_road.jpg","volvo_ev_crossover.jpg","dodge_challenger.jpg","landrover_defender.jpg"]
  -for(var i=0;i<3; i++) {
    -let rand = Math.floor(Math.random()*5)
    img(src=`images/${results[rand]}`,height="300px",width="300px")
  -}